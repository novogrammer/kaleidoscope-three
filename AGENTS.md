# プロジェクト作業ルール

## 基本方針

- 実装や設計判断の前に `docs/migration-premises.md` を確認する。
- 相談、調査、方針検討だけを求められた場合は、明示的に依頼されるまでコードやファイルを変更しない。
- 設計上の前提が変わった場合は、実装だけでなく関連ドキュメントも更新する。
- ドキュメント、設定、ソースコードに、開発者のホームディレクトリなどマシン固有の絶対パスを残さない。

## 参照元

- Unity版は `references/KaleidoscopeUnityHdr` から参照する。
- `references/` 以下は読み取り専用として扱い、移植作業によって変更しない。
- Unity版との厳密な画素一致は目標にせず、作品の意図と主要な挙動をthree.js向けに再構成する。

## プロジェクト構成

- Viteのrootは `src/` とする。
- 独立した確認ページは `src/<name>/` 以下に局所化し、`/<name>/index.html` で開ける構成にする。
- 新しいHTMLエントリーを追加するときは、必要に応じて `vite.config.ts` のビルド入力も更新する。
- 実行時にCDNや外部のモデル配布元へ依存しない。必要なWASM、モデル、fixtureなどはViteのアセットとして同梱する。
- 外部から取得したモデルやバイナリアセットは、取得元、バージョン、SHA-256を `README.md` に記録する。更新時は記録も同時に更新する。

## 描画と互換性

- WebGPUを優先しつつ、WebGL 2フォールバックを対応範囲に含める。
- WebGPUとWebGL 2で可能な限り同じTSLとアプリケーションロジックを共有する。
- HDR非対応環境でも、SDRとして主要表現が普通に閲覧できる状態を保つ。
- 紙吹雪はCPUで状態を更新し、`InstancedMesh` で描画する。GPGPUやWebGPU Computeを必須にしない。

## カメラとMediaPipe

- 通常利用では前面カメラを使用し、利用者が開始操作を行った後にだけ権限を要求する。
- 顔検出と描画を直接結合せず、正規化した共通データを介して連携する。
- 開発中とCodexによる通常確認ではWebカメラを必須にせず、URLパラメーターで選択できるfixture入力を使用する。
- カメラ拒否時や顔を検出できない場合にも、表示全体が停止しない構成を保つ。

## 検証

- 変更後は、まず `npm run build` と関連するVitestを実行する。
- ブラウザ確認は、描画、MediaPipe統合、ブラウザ固有機能など、ブラウザでなければ確認できない節目に限定する。
- 軽微な変更ごとにスクリーンショットやブラウザ自動操作を繰り返さない。
- 実Webカメラ、SafariおよびiOS固有動作、実ディスプレイでのHDR表示は、原則として手動確認項目として扱う。
- ブラウザ確認を行っていない場合は、作業結果でその旨を明記する。
