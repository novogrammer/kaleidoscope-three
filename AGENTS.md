# プロジェクト作業ルール

## 情報源

- 実装や設計判断の前に `docs/migration-premises.md` を確認し、設計上の唯一の基準として扱う。
- アセットの取得元、バージョン、ハッシュ、同梱方針は `README.md` を確認する。
- 前提やアセットを変更した場合は、実装と同時に該当する文書も更新する。
- 設計内容をこのファイルへ重複して記載しない。

## 作業境界

- 相談、調査、方針検討だけを求められた場合は、明示的に依頼されるまでコードやファイルを変更しない。
- `references/` 以下は読み取り専用として扱い、変更しない。
- ドキュメント、設定、ソースコードに、開発者のホームディレクトリなどマシン固有の絶対パスを残さない。

## 検証

- 変更後は、まず `npm run build` と関連するVitestを実行する。
- ブラウザ確認は、ブラウザでなければ確認できない節目に限定し、軽微な変更ごとに繰り返さない。
- 実Webカメラ、SafariおよびiOS固有動作、実ディスプレイでのHDR表示は、原則として手動確認項目として扱う。
- ブラウザ確認を行っていない場合は、作業結果でその旨を明記する。
